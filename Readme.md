### only once when it's a new folder.
git init

git remote add origin git@github.com:lidoravitan/lidor-test-123.git 

### dailiy commands 
git add .
git commit -m "commit text"
git status
git push
git pull

